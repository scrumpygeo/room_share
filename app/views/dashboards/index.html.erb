 <div class="container-fluid">
 <h3 class="text-center">Welcome <%= current_user.first_name%>!</h3>

 <h4 class="text-center pb-3">These are all your properties </h4>

  <div class="row">
    <% @rooms.each do |room| %>
    <div class="col-xs-12 col-sm-10 offset-sm-1">

      <div class="card-product mb-2">

      <%= link_to "Edit this room", edit_room_path(room),  class:"btn btn-outline-success" %>
      <%= link_to "Delete this room", rooms_path(room), method: :delete, class:"btn btn-outline-danger", data: {confirm: "Are you sure"} %>




        <%= room.name %>
     
          </div>

      </div>
    </div>
    <% end %>
</div>








 </div>
 