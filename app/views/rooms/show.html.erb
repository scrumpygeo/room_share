<h1 class="text-center mt-3"><%= @room.name %></h1>
<div class="container">
  <p><%= @room.description %></p>
  <p><%= @room.address %></p>
  <p><%= @room.city %></p>
  <p><%= @room.guest_nr %> guests</p>

 <p>€<%=  number_with_precision(@room.price, precision: 2) %>  </p>
  <%= image_tag @room.photo %>
</div>